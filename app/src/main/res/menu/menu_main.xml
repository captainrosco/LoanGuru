<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item android:id="@id/home"
        android:title = "Home"
        android:icon="@drawable/ic_action_action_home"
        app:showAsAction="always"/>
    <item android:id="@+id/amortization"
        android:title="amortization "
        android:icon="@drawable/ic_amortization"
        app:showAsAction="always"
        />
    <item android:id="@+id/affordability"
        android:title="Affordability"
        android:icon="@drawable/ic_action_av_equalizer"
        app:showAsAction="always"
        />
</menu>
